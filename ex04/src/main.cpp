#include "sed.hpp"



int	main(int argc, char *argv[])
{
	std::ifstream infile;
	std::ofstream outfile;

	if (argc != 4)
		return (EXIT_FAILURE);
	infile.open(argv[1]);
	if (infile.is_open())
	{
	}
}
